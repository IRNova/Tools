[
    {
        "project_name": "[CM Config](Default)",
        "hint_message": "Switched to CM project config, suitable for . project.",
        "path_template": {
            "PROXYIP": "proxyip={{IP:PORT}}",
            "SOCKS5": {
                "global": "socks5://{{IP:PORT}}",
                "standard": "socks5={{IP:PORT}}"
            },
            "HTTP": {
                "global": "http://{{IP:PORT}}",
                "standard": "http={{IP:PORT}}"
            }
        }
    },
    {
        "project_name": "[3K Config]",
        "hint_message": "Switched to 3K project config, suitable for project 6.",
        "path_template": {
            "PROXYIP": "proxyip={{IP:PORT}}",
            "SOCKS5": {
                "global": "socks5://{{IP:PORT}}?globalproxy",
                "standard": "socks5://{{IP:PORT}}"
            },
            "HTTP": {
                "global": "http://{{IP:PORT}}?globalproxy",
                "standard": "http://{{IP:PORT}}"
            }
        }
    },
    {
        "project_name": "eooce/Cloudflare-proxy[Lawang Project Config]",
        "hint_message": "Note! The Lawang project does not support non-global mode for SOCKS5/HTTP output.",
        "path_template": {
            "PROXYIP": "proxyip={{IP:PORT}}",
            "SOCKS5": {
                "global": "proxyip=socks5://{{IP:PORT}}",
                "standard": "proxyip=socks5://{{IP:PORT}}"
            },
            "HTTP": {
                "global": "proxyip=http://{{IP:PORT}}",
                "standard": "proxyip=http://{{IP:PORT}}"
            }
        }
    }
]
